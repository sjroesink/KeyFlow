---
phase: 01-audio-midi-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/App.css
  - src/index.css
  - src/vite-env.d.ts
  - src/types/audio.ts
  - src/types/song.ts
  - src/store/audioStore.ts
  - src/store/songStore.ts
  - src/utils/noteUtils.ts
  - src/utils/noteUtils.test.ts
  - src/test/setup.ts
  - src/test/mocks/webAudioMock.ts
  - src/test/fixtures/test.mid
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
  - SONG-01

must_haves:
  truths:
    - "Project builds and dev server starts without errors"
    - "Type definitions exist for audio detection and song model"
    - "Zustand stores exist for audio state and song state"
    - "Note conversion utilities pass unit tests"
    - "Test infrastructure runs with Web Audio API mocks available"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "pitchy"
    - path: "src/types/audio.ts"
      provides: "Audio detection type contracts"
      exports: ["DetectedPitch", "AudioPipelineState", "PitchDetectorConfig"]
    - path: "src/types/song.ts"
      provides: "Song model type contracts"
      exports: ["SongNote", "SongModel"]
    - path: "src/store/audioStore.ts"
      provides: "Zustand store for detected note state"
      exports: ["useAudioStore"]
    - path: "src/store/songStore.ts"
      provides: "Zustand store for loaded song state"
      exports: ["useSongStore"]
    - path: "src/utils/noteUtils.ts"
      provides: "Hz-to-note and Hz-to-MIDI conversion"
      exports: ["frequencyToNote", "frequencyToMidi", "isNoteMatch"]
    - path: "src/utils/noteUtils.test.ts"
      provides: "Unit tests for note conversions"
    - path: "src/test/mocks/webAudioMock.ts"
      provides: "Mock AudioContext, AnalyserNode, getUserMedia for tests"
  key_links:
    - from: "src/store/audioStore.ts"
      to: "src/types/audio.ts"
      via: "import type { DetectedPitch }"
      pattern: "import.*from.*types/audio"
    - from: "src/store/songStore.ts"
      to: "src/types/song.ts"
      via: "import type { SongModel }"
      pattern: "import.*from.*types/song"
    - from: "src/utils/noteUtils.ts"
      to: "tonal"
      via: "import { Note } from 'tonal'"
      pattern: "import.*Note.*from.*tonal"
---

<objective>
Scaffold the Vite + React + TypeScript project, define all shared type contracts and Zustand stores, set up test infrastructure with Web Audio API mocks, and create the note utility module with tests.

Purpose: Establish the project foundation so that Plan 02 (audio pipeline) and Plan 03 (MIDI parser) can execute in parallel against well-defined type contracts and a working test harness.

Output: A buildable project with types, stores, utilities, test mocks, and a passing test suite.
</objective>

<execution_context>
@C:/Users/sjroe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sjroe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-midi-foundation/01-RESEARCH.md
@.planning/phases/01-audio-midi-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project with all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/App.css, src/index.css, src/vite-env.d.ts, src/test/setup.ts</files>
  <read_first>
    - .planning/phases/01-audio-midi-foundation/01-RESEARCH.md (Standard Stack section for exact versions and install commands)
  </read_first>
  <action>
    1. Run `npm create vite@latest . -- --template react-ts` in the project root (D:\Projects\WebPianoLearner). Since the directory already has files (.planning/), use the current directory as target.

    2. Install Phase 1 production dependencies:
       `npm install pitchy @tonejs/midi tonal zustand`

    3. Install Tailwind CSS v4:
       `npm install tailwindcss @tailwindcss/vite`

    4. Install dev dependencies:
       `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom`

    5. Update vite.config.ts to include Tailwind plugin and Vitest config:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         test: {
           globals: true,
           environment: 'jsdom',
           setupFiles: ['./src/test/setup.ts'],
         },
       })
       ```

    6. Add Tailwind import to src/index.css:
       ```css
       @import "tailwindcss";
       ```

    7. Create src/test/setup.ts with:
       ```typescript
       import '@testing-library/jest-dom/vitest';
       ```

    8. Update the default App.tsx to a minimal shell:
       ```tsx
       function App() {
         return (
           <div className="min-h-screen bg-gray-950 text-white flex flex-col items-center justify-center p-4">
             <h1 className="text-3xl font-bold mb-8">WebPianoLearner</h1>
             <p className="text-gray-400">Phase 1: Audio + MIDI Foundation</p>
           </div>
         )
       }
       export default App
       ```

    9. Verify the project builds: `npm run build`
    10. Verify dev server starts: `npm run dev` (start and immediately stop, just check no errors)
  </action>
  <verify>
    <automated>cd D:/Projects/WebPianoLearner && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "pitchy" in dependencies
    - package.json contains "@tonejs/midi" in dependencies
    - package.json contains "tonal" in dependencies
    - package.json contains "zustand" in dependencies
    - package.json contains "vitest" in devDependencies
    - package.json contains "@testing-library/react" in devDependencies
    - vite.config.ts contains "tailwindcss" in plugins array
    - vite.config.ts contains "environment: 'jsdom'" in test config
    - src/test/setup.ts contains "@testing-library/jest-dom/vitest"
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Project scaffolded, all dependencies installed, builds without errors, Vitest configured with jsdom environment</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define type contracts, Zustand stores, and note utilities with tests</name>
  <files>src/types/audio.ts, src/types/song.ts, src/store/audioStore.ts, src/store/songStore.ts, src/utils/noteUtils.ts, src/utils/noteUtils.test.ts, src/test/mocks/webAudioMock.ts</files>
  <read_first>
    - .planning/phases/01-audio-midi-foundation/01-RESEARCH.md (Pattern 3: Hz-to-Note Conversion, Pattern 4: MIDI SongModel, Zustand Audio Store example)
    - D:/Projects/WebPianoLearner/vite.config.ts (verify test config present)
    - D:/Projects/WebPianoLearner/package.json (verify dependencies installed)
  </read_first>
  <behavior>
    - frequencyToNote(440) returns "A4"
    - frequencyToNote(261.63) returns "C4"
    - frequencyToMidi(440) returns 69
    - frequencyToMidi(261.63) returns 60
    - isNoteMatch(440, 69, 50) returns true (A4 matches MIDI 69)
    - isNoteMatch(440, 70, 50) returns false (A4 does not match MIDI 70 = A#4)
    - isNoteMatch(442, 69, 50) returns true (slightly sharp A4 still matches within 50 cents)
    - frequencyToNote for frequencies below 27.5 Hz or above 4186 Hz should still return a value (edge case)
  </behavior>
  <action>
    1. Create src/types/audio.ts:
       ```typescript
       export interface DetectedPitch {
         frequency: number;
         clarity: number;
         noteName: string;
         midiNumber: number;
       }

       export type AudioPipelineStatus = 'idle' | 'requesting' | 'listening' | 'error';

       export interface AudioPipelineState {
         status: AudioPipelineStatus;
         error: string | null;
       }

       export interface PitchDetectorConfig {
         clarityThreshold: number;  // 0-1, default 0.9
         minFrequency: number;      // Hz, default 27.5 (A0)
         maxFrequency: number;      // Hz, default 4186 (C8)
       }

       export const DEFAULT_PITCH_CONFIG: PitchDetectorConfig = {
         clarityThreshold: 0.9,
         minFrequency: 27.5,
         maxFrequency: 4186,
       };
       ```

    2. Create src/types/song.ts:
       ```typescript
       export interface SongNote {
         midi: number;        // MIDI number (60 = C4)
         name: string;        // "C4", "F#3", etc.
         startTime: number;   // seconds
         duration: number;    // seconds
         velocity: number;    // 0-1 normalized
         track: number;       // track index
       }

       export interface SongModel {
         name: string;
         duration: number;         // total duration in seconds
         bpm: number;
         timeSignature: [number, number];
         notes: SongNote[];        // sorted by startTime
         trackCount: number;
         trackNames: string[];
       }
       ```

    3. Create src/utils/noteUtils.ts:
       ```typescript
       import { Note } from 'tonal';

       /** Convert Hz to nearest note name (e.g. 440 -> "A4") */
       export function frequencyToNote(hz: number): string {
         return Note.fromFreq(hz);
       }

       /** Convert Hz to MIDI number for comparison with MIDI file data */
       export function frequencyToMidi(hz: number): number {
         return Math.round(12 * Math.log2(hz / 440) + 69);
       }

       /**
        * Check if detected frequency matches expected MIDI note within tolerance.
        * @param detectedHz - detected frequency in Hz
        * @param expectedMidi - expected MIDI note number
        * @param toleranceCents - tolerance in cents (default 50 = half semitone)
        */
       export function isNoteMatch(
         detectedHz: number,
         expectedMidi: number,
         toleranceCents: number = 50
       ): boolean {
         const detectedMidi = 12 * Math.log2(detectedHz / 440) + 69;
         const diff = Math.abs(detectedMidi - expectedMidi);
         return diff < toleranceCents / 100;
       }
       ```

    4. Write tests FIRST in src/utils/noteUtils.test.ts, then verify they fail, then ensure implementation passes:
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { frequencyToNote, frequencyToMidi, isNoteMatch } from './noteUtils';

       describe('frequencyToNote', () => {
         it('converts 440 Hz to A4', () => {
           expect(frequencyToNote(440)).toBe('A4');
         });
         it('converts 261.63 Hz to C4', () => {
           expect(frequencyToNote(261.63)).toBe('C4');
         });
         it('converts 329.63 Hz to E4', () => {
           expect(frequencyToNote(329.63)).toBe('E4');
         });
       });

       describe('frequencyToMidi', () => {
         it('converts 440 Hz to MIDI 69', () => {
           expect(frequencyToMidi(440)).toBe(69);
         });
         it('converts 261.63 Hz to MIDI 60', () => {
           expect(frequencyToMidi(261.63)).toBe(60);
         });
         it('converts 880 Hz to MIDI 81', () => {
           expect(frequencyToMidi(880)).toBe(81);
         });
       });

       describe('isNoteMatch', () => {
         it('returns true for exact match (440 Hz = MIDI 69)', () => {
           expect(isNoteMatch(440, 69)).toBe(true);
         });
         it('returns false for adjacent semitone (440 Hz != MIDI 70)', () => {
           expect(isNoteMatch(440, 70)).toBe(false);
         });
         it('returns true for slightly sharp within tolerance (442 Hz ~ MIDI 69)', () => {
           expect(isNoteMatch(442, 69, 50)).toBe(true);
         });
         it('returns true for slightly flat within tolerance (438 Hz ~ MIDI 69)', () => {
           expect(isNoteMatch(438, 69, 50)).toBe(true);
         });
       });
       ```

    5. Create src/store/audioStore.ts:
       ```typescript
       import { create } from 'zustand';
       import type { DetectedPitch, AudioPipelineStatus } from '../types/audio';
       import { frequencyToNote, frequencyToMidi } from '../utils/noteUtils';

       interface AudioStoreState {
         status: AudioPipelineStatus;
         error: string | null;
         detectedPitch: DetectedPitch | null;
         isListening: boolean;
         setStatus: (status: AudioPipelineStatus, error?: string | null) => void;
         setDetectedNote: (frequency: number | null, clarity: number) => void;
       }

       export const useAudioStore = create<AudioStoreState>((set) => ({
         status: 'idle',
         error: null,
         detectedPitch: null,
         isListening: false,
         setStatus: (status, error = null) => set({
           status,
           error,
           isListening: status === 'listening',
         }),
         setDetectedNote: (frequency, clarity) => set({
           detectedPitch: frequency
             ? {
                 frequency,
                 clarity,
                 noteName: frequencyToNote(frequency),
                 midiNumber: frequencyToMidi(frequency),
               }
             : null,
         }),
       }));
       ```

    6. Create src/store/songStore.ts:
       ```typescript
       import { create } from 'zustand';
       import type { SongModel } from '../types/song';

       interface SongStoreState {
         song: SongModel | null;
         isLoading: boolean;
         error: string | null;
         setSong: (song: SongModel) => void;
         setLoading: (isLoading: boolean) => void;
         setError: (error: string) => void;
         clearSong: () => void;
       }

       export const useSongStore = create<SongStoreState>((set) => ({
         song: null,
         isLoading: false,
         error: null,
         setSong: (song) => set({ song, isLoading: false, error: null }),
         setLoading: (isLoading) => set({ isLoading }),
         setError: (error) => set({ error, isLoading: false }),
         clearSong: () => set({ song: null, error: null }),
       }));
       ```

    7. Create src/test/mocks/webAudioMock.ts with mock classes for AudioContext, AnalyserNode, MediaStream, and navigator.mediaDevices.getUserMedia:
       ```typescript
       import { vi } from 'vitest';

       export class MockAnalyserNode {
         fftSize = 2048;
         frequencyBinCount = 1024;
         context = { sampleRate: 44100 };

         getFloatTimeDomainData(buffer: Float32Array): void {
           // Fill with silence by default
           buffer.fill(0);
         }

         connect() { return this; }
         disconnect() {}
       }

       export class MockAudioContext {
         state: AudioContextState = 'running';
         sampleRate = 44100;

         private _analyser = new MockAnalyserNode();

         async resume(): Promise<void> {
           this.state = 'running';
         }

         async close(): Promise<void> {
           this.state = 'closed';
         }

         createAnalyser(): MockAnalyserNode {
           return this._analyser;
         }

         createMediaStreamSource(_stream: MediaStream) {
           return {
             connect: vi.fn(),
             disconnect: vi.fn(),
           };
         }
       }

       export class MockMediaStream {
         getTracks() {
           return [{ stop: vi.fn(), kind: 'audio' }];
         }
       }

       export function setupWebAudioMocks() {
         global.AudioContext = MockAudioContext as unknown as typeof AudioContext;
         global.navigator = {
           ...global.navigator,
           mediaDevices: {
             getUserMedia: vi.fn().mockResolvedValue(new MockMediaStream()),
           },
         } as unknown as Navigator;
       }

       export function teardownWebAudioMocks() {
         vi.restoreAllMocks();
       }
       ```

    8. Create a small test MIDI file fixture at src/test/fixtures/test.mid. Generate it programmatically using @tonejs/midi in a Node script or include a minimal binary. The fixture should contain: 1 track with 4 notes (C4, D4, E4, F4), each 0.5 seconds duration, BPM 120, 4/4 time signature. Create a small script to generate this:
       - Create a temporary generate-fixture.mjs script in the project root
       - Run it with `node generate-fixture.mjs`
       - Delete the script after generating the .mid file
  </action>
  <verify>
    <automated>cd D:/Projects/WebPianoLearner && npx vitest run src/utils/noteUtils.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/types/audio.ts contains "export interface DetectedPitch"
    - src/types/audio.ts contains "export type AudioPipelineStatus"
    - src/types/audio.ts contains "export interface PitchDetectorConfig"
    - src/types/audio.ts contains "export const DEFAULT_PITCH_CONFIG"
    - src/types/song.ts contains "export interface SongNote"
    - src/types/song.ts contains "export interface SongModel"
    - src/store/audioStore.ts contains "export const useAudioStore"
    - src/store/audioStore.ts imports from "../types/audio"
    - src/store/songStore.ts contains "export const useSongStore"
    - src/store/songStore.ts imports from "../types/song"
    - src/utils/noteUtils.ts contains "export function frequencyToNote"
    - src/utils/noteUtils.ts contains "export function frequencyToMidi"
    - src/utils/noteUtils.ts contains "export function isNoteMatch"
    - src/utils/noteUtils.test.ts exists and all tests pass (npx vitest run src/utils/noteUtils.test.ts exits 0)
    - src/test/mocks/webAudioMock.ts contains "export class MockAudioContext"
    - src/test/mocks/webAudioMock.ts contains "export class MockAnalyserNode"
    - src/test/mocks/webAudioMock.ts contains "export function setupWebAudioMocks"
    - src/test/fixtures/test.mid exists and is a valid MIDI file (non-zero size)
    - `npm run build` still exits 0 after all files added
  </acceptance_criteria>
  <done>All type contracts defined, both Zustand stores created, noteUtils passes all unit tests, Web Audio mocks ready for Plan 02, test MIDI fixture ready for Plan 03</done>
</task>

</tasks>

<verification>
1. `npm run build` passes (project compiles)
2. `npx vitest run --reporter=verbose` passes (noteUtils tests green)
3. All type files export the expected interfaces (grep confirms)
4. Both stores are importable and functional
5. Web Audio mocks are ready for Plan 02 audio tests
6. Test MIDI fixture exists for Plan 03 parser tests
</verification>

<success_criteria>
- Project builds without errors
- Dev server starts without errors
- All noteUtils tests pass
- Type contracts exist for audio and song domains
- Zustand stores exist for audio and song state
- Web Audio API mocks available for subsequent plans
- Test MIDI fixture available for subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-midi-foundation/01-01-SUMMARY.md`
</output>
